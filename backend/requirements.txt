# AmmiAI backend runtime deps. Trimmed for a lean, reliable Render build —
# the previous list carried ~11 unused Emergent-template packages (pandas,
# numpy, boto3, jq, typer, passlib, bcrypt, python-jose, requests,
# requests-oauthlib, cryptography) that nothing in backend/*.py imports.
# Dev/test tools (pytest, black, isort, flake8, mypy) are intentionally NOT
# here — install them separately when running the tests/ suite locally.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
python-dotenv>=1.0.1
python-multipart>=0.0.9
httpx>=0.27.0
anthropic>=0.39.0
tzdata>=2024.2
